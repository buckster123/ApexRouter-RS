//! OWNER: unit S-04 (server/src/api/{rig,fit,catalog,usage,requests,jobs}.rs, server/src/jobs.rs). Do not edit outside that unit.
//!
//! `GET /v1/fit` (query form) and `POST /v1/fit` (body = `FitInput`). The same pure function the CLI, the MCP tool and both Launch drawers call.
